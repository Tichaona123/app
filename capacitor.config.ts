import { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig={ appId:"com.deutschbuddy.app", appName:"DeutschBuddy", webDir:"dist", bundledWebRuntime:false };
export default config; 